const NAME_OF_PLANET = 'Earth'; // the name of the planet is known prior to runtime, so all caps name (naming it EARTH then assigning Earth to that felt redundant)

const currentVisitor; //Since this is unknown prior to runtime, but wont change during the session, can be const named in camel case
